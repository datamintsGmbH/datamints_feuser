plugin.tx_datamintsfeuser_pi1 {

	showtype = register
	usedfields = username, password, image, --separator--, first_name, last_name, address, zip, city, --separator--, email, www, --submit--
	requiredfields = username, password, first_name, last_name, email
	uniquefields = username, email

	register {
		sendusermail = 1
		sendadminmail = 1
	}

	validate {
		username.type = username
		password.type = password
		password.length = 6
		email.type = email
	}

}
